import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function ConfigPage() {
  return (
    <PagePlaceholder
      title="Configurações"
      description="Dados fiscais do tenant, equipe, conexões de canais e preferências gerais."
    />
  );
}
