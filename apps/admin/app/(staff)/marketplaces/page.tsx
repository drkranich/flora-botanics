import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function MarketplacesPage() {
  return (
    <PagePlaceholder
      title="Marketplaces"
      description="Conexões com Shopee, Instagram/WhatsApp e Mercado Livre, sincronização de estoque e anúncios. Tabelas `channel_accounts` e `marketplace_listings` já existem no banco. Ordem de prioridade definida na Seção 14 do blueprint."
    />
  );
}
