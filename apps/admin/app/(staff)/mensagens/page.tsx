import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function MensagensPage() {
  return (
    <PagePlaceholder
      title="Mensagens"
      description="Inbox omnichannel (WhatsApp, Instagram, e-mail), templates e automações (aniversário, carrinho abandonado, remarketing). Tabelas `conversations`, `messages`, `message_templates`, `automations` e `automation_runs` já existem no banco."
    />
  );
}
