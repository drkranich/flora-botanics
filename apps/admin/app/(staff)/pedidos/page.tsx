import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function PedidosPage() {
  return (
    <PagePlaceholder
      title="Pedidos"
      description="Vendas, cancelamentos, devoluções e máquina de estados dos pedidos — site e marketplaces. Tabela `orders` já existe no banco."
    />
  );
}
